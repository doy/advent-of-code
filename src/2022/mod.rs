use crate::prelude::*;

// NEXT MOD

pub fn run(day: u8, puzzle: u8) -> Result<i64> {
    match (day, puzzle) {
        // NEXT PART
        _ => Err(anyhow!("unknown puzzle {}-{}", day, puzzle)),
    }
}
